# v0.7.2 Design — Background Agent Wiring & Embedded SearXNG Setup

**Date:** 2026-03-21
**Branch:** `feat/0.7.2`
**Depends on:** v0.7.1 (BackgroundAgentManager, SearchBackend trait, SearxngBackend, caboose-server)

---

## Overview

v0.7.2 connects two pieces of infrastructure that shipped as stubs in v0.7.1:

1. **Background agent wiring** — makes `/bg` functional by spawning real `AgentLoop` instances via `BackgroundAgentManager`, and adds a `spawn_background` tool so the LLM can initiate background work autonomously.
2. **Embedded SearXNG setup** — bundles Docker Compose config for SearXNG + Redis into Caboose so users get web search with a single command, no repo cloning.

---

## Feature 1: Background Agent Wiring

### 1.1 Entry Points

Two ways to spawn a background agent:

1. **`/bg <prompt>`** — explicit user command. Spawns immediately with the active provider/model unless `--model <model>` is specified.
2. **`spawn_background` tool** — LLM-initiated. The agent decides when a task should run in the background, same pattern as the existing `spawn_agent` tool for subagents. The LLM receives system prompt guidance on when to use it.

### 1.2 System Prompt Guidance

Added to the existing subagents block in the system prompt:

> You also have a `spawn_background` tool. Use it for tasks that are independent of the current conversation and don't need interactive feedback — long test runs, large refactors across many files, code generation tasks where the user doesn't need to watch progress. Don't use it for tasks where the user is waiting for the answer. Background agents run with auto-approve and report results when done.

### 1.3 Spawn Flow

1. Check `BackgroundAgentManager::can_spawn()` — respects `max_concurrent` and `global_budget` limits.
2. If denied, post system message explaining why (budget exceeded or concurrency limit).
3. Create a new `AgentLoop` with:
   - The specified (or active) provider/model
   - Auto-approve permission mode (no tool approval prompts)
   - The user's prompt as the initial message
   - A unique agent ID (UUID)
4. Call `BackgroundAgentManager::register()` — emits `CoreEvent::BackgroundAgentStarted`.
5. Spawn on a tokio task. Store the `JoinHandle` via `store_handle()`.
6. The spawned task:
   - Runs the agent loop to completion
   - Calls `update_tokens()` periodically
   - On success: calls `mark_complete()`, emits `BackgroundAgentComplete`
   - On failure: calls `mark_failed()`, emits `BackgroundAgentFailed`

### 1.4 Provider/Model Resolution

`/bg` defaults to the active provider and model in the current session. The `--model` flag accepts any model ID that Caboose supports. Resolution follows the same cascade as the main session:

- If the model ID matches a known provider (e.g. `claude-haiku-4-5` → anthropic), use that provider's API key.
- If the model ID is an OpenRouter model path (e.g. `anthropic/claude-haiku-4-5`), route through OpenRouter.
- If the model ID doesn't resolve, post an error message.

The `spawn_background` tool accepts an optional `model` parameter with the same resolution logic.

### 1.5 Notification

- **Sidebar:** The existing agents section in the sidebar shows background agents with status (Running / Complete / Failed) and token usage. Reuses the same rendering pattern as subagents.
- **System message:** On completion or failure, a `ChatMessage::System` is posted to the main chat with: agent prompt (truncated), status, token usage, files modified (if any).

### 1.6 Commands

| Command | Action |
|---------|--------|
| `/bg <prompt>` | Spawn a background agent with the given prompt |
| `/bg --model <id> <prompt>` | Spawn with a specific model |
| `/bg list` | Show all background agents (running, complete, failed) with token usage |
| `/bg kill <id>` | Abort a running background agent |

### 1.7 Config

The existing `[background_agents]` TOML section (added in v0.7.1) is wired into `BackgroundAgentConfig`:

```toml
[background_agents]
max_agents = 3          # max concurrent (default: 5 in BackgroundAgentConfig)
per_agent_budget = 100000   # tokens per agent (default: 100_000)
global_budget = 500000      # total tokens across all agents (default: 500_000)
```

### 1.8 Files to Modify

- `core/src/background.rs` — no changes needed, API is complete
- `tui/src/app/state.rs` — initialize `background_manager` from config instead of `None`
- `tui/src/app/slash_commands.rs` — replace `/bg` stubs with real spawn/list/kill logic
- `tui/src/agent/tools.rs` — add `spawn_background` tool definition and execution
- `tui/src/app/mod.rs` — poll `BackgroundAgentManager` events in the event loop, post system messages on completion
- `tui/src/tui/sidebar.rs` — render background agents in sidebar (reuse subagent pattern)
- `tui/src/app/state.rs` — system prompt update for `spawn_background` guidance

---

## Feature 2: Embedded SearXNG Setup

### 2.1 Overview

Caboose embeds the minimal Docker Compose configuration for SearXNG + Redis and manages the lifecycle. Zero user inputs required. No Cloudflare Tunnel — local only, architected so remote access is addable later via config.

### 2.2 Entry Points

1. **`/init`** — after generating CABOOSE.md, offers: "Set up web search? Requires Docker (y/n)". If yes, runs the setup flow.
2. **`/search-setup`** — standalone command for users who skipped init or want to set it up later.
3. **`/search-setup stop`** — runs `docker compose down` to tear down containers.
4. **`/search-setup status`** — checks if SearXNG containers are running and healthy.

### 2.3 Generated Files

Written to `~/.config/caboose/search/`:

**`docker-compose.yml`:**
```yaml
services:
  redis:
    image: redis:7-alpine
    container_name: caboose-redis
    restart: unless-stopped

  searxng:
    image: searxng/searxng:2026.3.10-8b95b2058
    container_name: caboose-searxng
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./searxng:/etc/searxng
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/search?q=test&format=json"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

Key differences from tender-highball:
- Bound to `127.0.0.1:8080` (not `0.0.0.0`) — local only
- No Cloudflare Tunnel container
- Container names prefixed with `caboose-` to avoid collisions

**`searxng/settings.yml`:**
Generated with:
- Random secret key (32-byte hex, generated via `rand` crate)
- `base_url: "http://127.0.0.1:8080"`
- `public_instance: false`
- JSON format enabled
- Google + Bing engines (DuckDuckGo disabled by default due to CAPTCHA issues)
- Redis URL pointing to the companion container

### 2.4 Setup Flow

1. Check `docker compose` is available (run `docker compose version`).
2. If not found, post system message: "Docker is required for web search. Install it from docker.com"
3. Create `~/.config/caboose/search/` and `~/.config/caboose/search/searxng/` directories.
4. Generate secret key, write `docker-compose.yml` and `searxng/settings.yml`.
5. Run `docker compose up -d` from the search directory.
6. Poll `http://127.0.0.1:8080/search?q=test&format=json` until healthy (max 30s, 1s intervals).
7. On success: auto-write to global config (`~/.config/caboose/config.toml`):
   ```toml
   [services.web_search]
   provider = "searxng"
   base_url = "http://127.0.0.1:8080"
   ```
8. Post system message: "Web search enabled. SearXNG running on localhost:8080."
9. On timeout: post error with suggestion to check `docker compose logs -f caboose-searxng`.

### 2.5 Idempotency

If `~/.config/caboose/search/docker-compose.yml` already exists:
- `/search-setup` checks if containers are running. If yes, posts "Web search is already running." If not, starts them.
- Never overwrites existing config files (preserves user customizations to settings.yml).

### 2.6 Remote Access (Future)

The architecture supports adding remote access later without code changes:
- User adds a Cloudflare Tunnel container to the compose file manually
- Or changes `bind` from `127.0.0.1` to `0.0.0.0` and configures their own reverse proxy
- The `[services.web_search]` config already supports any `base_url`

### 2.7 Files to Create/Modify

- `core/src/search_setup.rs` (new) — compose file generation, settings generation, Docker health check, config auto-write
- `tui/src/app/slash_commands.rs` — add `/search-setup` command handler, modify `/init` to offer search setup
- `tui/src/tui/command.rs` — register `/search-setup` in command registry

---

## Error Handling

| Scenario | Behavior |
|----------|----------|
| Docker not installed | "Docker is required for web search. Install it from docker.com" |
| Docker running but compose fails | Show compose stderr output |
| Health check timeout (30s) | "SearXNG failed to start. Check logs with: docker compose -f ~/.config/caboose/search/docker-compose.yml logs" |
| `/bg` with budget exhausted | "Background agent budget exceeded. Increase in [background_agents] config or wait for running agents to complete" |
| `/bg` with concurrency limit hit | "Maximum concurrent background agents reached ({n}/{max}). Wait for one to finish or use /bg kill" |
| `/bg kill` with invalid id | "No background agent with id '{id}'" |
| `/bg --model` with unknown model | "Unknown model '{id}'. Use /model to see available models" |
| SearXNG containers already running | "Web search is already running on localhost:8080" |

---

## Testing

### Background Agents

- Unit test: `spawn_background` tool definition has correct schema (prompt required, model optional)
- Unit test: `/bg list` output formatting (empty, one running, mixed states)
- Unit test: `/bg kill` with valid and invalid IDs
- Unit test: `--model` flag parsing from `/bg` input
- Integration: budget enforcement blocks spawn when exceeded (existing BackgroundAgentManager tests cover the manager itself)
- Integration: system message posted on agent completion

### SearXNG Setup

- Unit test: `docker-compose.yml` generation produces valid YAML with correct container names, ports, and bind address
- Unit test: `settings.yml` generation includes random secret key, correct redis URL, JSON format enabled
- Unit test: config auto-write appends `[services.web_search]` to existing config without clobbering other sections
- Unit test: idempotency — existing files not overwritten
- Mock test: Docker not available returns appropriate error
- Mock test: health check timeout returns appropriate error

---

## Out of Scope

- `/bg attach` / `/bg detach` — deferred, revisit if users request it
- Cloudflare Tunnel integration — users who want remote access can modify compose files manually
- Auto-detecting when to background a task — the LLM decides via `spawn_background` tool; no heuristic-based auto-detection
- `/settings` UI for background agent config — TOML config is sufficient for now
- Background agents running across sessions — agents are session-scoped, killed on `/new` or quit
